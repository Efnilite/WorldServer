name: WorldServer
version: ${project.version}
api-version: 1.16
author: Efnilite
main: dev.efnilite.ws.WorldServer
description: Separate chat, the tab list and the economy in each world.
softdepend: [Vault, PlaceholderAPI]
commands:
  worldserver:
    aliases: [ws]
    description: The main command of WorldServer.